# Tools

`tools/` contains development and benchmarking utilities that are not shipping binaries.

- `scripts/`: local cluster orchestration, recovery harnesses, proto generation, debugging, and benchmark runners
- `bench/`: YCSB-based benchmark harness in its own Go module
